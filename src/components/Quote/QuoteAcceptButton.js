export const QuoteBlueButton = ({title}) => {
    return (
        <button>
            <div className='form_buttons__item accept'>
                {title}
            </div>
        </button>
    )
}